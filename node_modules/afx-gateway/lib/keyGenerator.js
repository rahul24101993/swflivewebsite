const selfsigned = require( 'selfsigned' );
const fs = require( 'fs' );

var pems = selfsigned.generate( null, { keySize: 2048, days: 30, algorithm: 'sha256' } );
fs.writeFileSync( 'privateKey.pem', pems.private );
fs.writeFileSync( 'publickey.pem', pems.public );
