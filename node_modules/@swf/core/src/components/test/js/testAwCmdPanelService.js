/**
 * render function for AwBreak
 * @deprecated afx@4.2.0.
 * @alternative <br>
 * @obsoleteIn afx@5.1.0
 * @returns {JSX.Element} react component
 */
import AwCommandSubPanel from 'viewmodel/AwCommandSubPanelViewModel';
import eventBus from 'js/eventBus';

export const awTestCommandSubPanelRenderFn = () => {
    return <AwCommandSubPanel panelId='CommandSubPanel1'></AwCommandSubPanel>;
};

export const loadSubPanel = ()=>{
    eventBus.publish( 'awPanel.navigate', {
        destPanelId: 'CmdSubPanel2',
        recreatePanel: false,
        supportGoBack: true,
        title:'add'
    } );
}
;
