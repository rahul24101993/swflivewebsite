import AwWidget from 'viewmodel/AwWidgetViewModel';

/**
 * render function for AwPropertyRenderingHint
 * @deprecated afx@4.2.0.
 * @alternative <AwWidget>
 * @obsoleteIn afx@5.1.0
 *
 * @param {*} param0 context for render function interpolation
 * @returns {JSX.Element} react component
 */
export const awPropertyRenderingHintRenderFunction = ( props ) => {
    const {
        viewModel,
        ctxMin,
        actions,
        i18n,
        fields,
        grids,
        formProp,
        messages,
        ...prop
    } = props;
    const { hint } = prop;
    return <AwWidget {...prop} renderingHint={hint}></AwWidget>;
};
