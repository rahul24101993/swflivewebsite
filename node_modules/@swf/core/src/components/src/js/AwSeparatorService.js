export const awSeparatorRenderFn = ( props ) => {
    const { isVertical } = props;
    return <div className={ `aw-theme-separator ${isVertical ? 'aw-vertical-separator' : 'aw-xrt-separator'}` }></div>;
};
