/**
 * render function for AwForm
 * @param {*} param0 context for render function interpolation
 * @returns {JSX.Element} react component
 */

export const awFormRenderFunction = ( props ) => {
    return (
        <form className='aw-layout-panelContent' noValidate>{props.children}</form>
    );
};
