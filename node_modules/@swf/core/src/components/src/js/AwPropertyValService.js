import AwWidgetVal from 'viewmodel/AwWidgetValViewModel';
import { getValClass } from 'js/componentUtils';

/**
 * render function for AwPropertyVal
 * @deprecated afx@4.3.0.
 * @alternative <AwWidget>
 * @obsoleteIn afx@6.0.0
 *
 * @param {*} param0 context for render function interpolation
 * @returns {JSX.Element} react component
 */
export const awPropertyValRenderFunction = ( props ) => {
    const {
        viewModel,
        ctxMin,
        actions,
        i18n,
        fields,
        grids,
        formProp,
        messages,
        ...prop
    } = props;

    return <AwWidgetVal {...prop} className={ getValClass( prop )}></AwWidgetVal>;
};
