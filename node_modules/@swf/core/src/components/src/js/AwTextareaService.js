import AwPropertyLabel from 'viewmodel/AwPropertyLabelViewModel';
import AwTextareaVal from 'viewmodel/AwTextareaValViewModel';

/**
 * render function for AwTextarea
 * @param {*} param0 context for render function interpolation
 * @returns {JSX.Element} react component
 */
export const awTextareaRenderFunction = ( props ) => {
    const {
        viewModel,
        ctxMin,
        actions,
        i18n,
        fields,
        grids,
        formProp,
        messages,
        ...prop
    } = props;
    return (
        <AwPropertyLabel {...prop}>
            <AwTextareaVal {...prop} className=''/>
        </AwPropertyLabel>
    );
};
