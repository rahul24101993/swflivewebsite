// Copyright (c) 2021 Siemens

/**
  * equivalent to schema.json
  * during build time following string will be replaced by processed schema templates files.
  *
  * @module js/schema
  */
const schema = '//schema';
export const getSchema = () => {
    return schema;
};
