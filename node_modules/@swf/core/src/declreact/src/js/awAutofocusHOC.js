// Copyright (c) 2021 Siemens
/* eslint-disable react/display-name */

/**
 * @module js/awAutofocusHOC
 */
import React from 'react';

const displayName = 'AwAutoFocus';

const awAutofocusHOC = ( Component ) => {
    // https://reactjs.org/docs/forwarding-refs.html#forwarding-refs-to-dom-components
    return React.forwardRef( ( { children, ...props }, ref ) => {
        let { awAutofocus } = props;
        let finalProps = { ...props };
        if( awAutofocus ) {
            finalProps.autoFocus = awAutofocus;
        }
        if( typeof Component === 'object' ) {
            Component.displayName = displayName;
        }
        return (
            <Component {...finalProps}>{children}</Component>
        );
    } );
};

export default awAutofocusHOC;
